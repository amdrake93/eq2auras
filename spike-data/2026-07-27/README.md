# Class-signature source logs — 2026-07-27

Raw EQ2/ACT combat logs + a `name → subclass` ground-truth key, ferried in to find
class **ability-signatures** (the `ability→class` ruleset behind class colors).

Input (the log) + known output (the class key) → then design the inference between them.
Character names are real (public in-game; Alex OK'd raw). Design context:
`docs/plans/2026-07-27-class-colors-palette.md` §Still open.
